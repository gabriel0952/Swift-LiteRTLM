# LiteRT-LM Fork Workspace

Reserved for the future `LiteRT-LM` fork used for runtime, sampler, and XCFramework build experiments.

Keep upstream fork code isolated here so the Swift Package at the repository root remains consumable on its own.
