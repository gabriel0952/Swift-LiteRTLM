# LiteRT Fork Workspace

Reserved for the future `LiteRT` fork used for GPU accelerator and runtime packaging work.

This is intentionally separate from the root Swift Package and from `Forks/LiteRT-LM/`.
